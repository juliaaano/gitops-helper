# Generated from rhdp-gitops-patterns/skeleton (v1.0.0)
---
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: {{ .Values.username }}-mesh
  labels:
    app: backend
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  selector:
    app: backend
  ports:
  - port: {{ .Values.backend.port }}
    targetPort: {{ .Values.backend.port }}
    protocol: TCP
